"""
PrivAgent Backend Package
Milestone 7+ Target: Receives ONLY sanitized context and structured browser coordinates.
Security Invariant: The backend must NEVER receive raw, unredacted visual context or sensitive PII.
"""

__version__ = "0.1.0"
